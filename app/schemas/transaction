from pydantic import BaseModel
from datetime import datetime

class TransactionSchema(BaseModel):
    date: datetime
    weight: int
    price: int
    total: int
    dealer_id: int
    farmer_id: int

    class Config:
        orm_mode = True